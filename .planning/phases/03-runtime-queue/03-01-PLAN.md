---
phase: 03-runtime-queue
plan: "01"
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - skills/skill-evolver/scripts/evolver.py
  - skills/skill-evolver/tests/test_capture.py
  - skills/skill-evolver/references/runtime.json
  - skills/skill-evolver/SKILL.md
  - hooks/hooks.json
  - README.md
  - docs/release-reports/runtime-queue.json
autonomous: true
requirements:
  - CAPT-01
user_setup: []
must_haves:
  truths:
    - trusted Stop Hook은 transcript 본문을 읽지 않고 한 session당 한 row로 metadata를 silent하게 queue한다.
    - repeated Stop과 DB contention은 idempotent upsert와 bounded authenticated spool로 수렴한다.
    - status는 SQLite를 read-only로 열고 transcript나 spool payload를 열거나 변경하지 않는다.
    - capture, maintenance와 report 생성은 installed skill, staging 또는 snapshot을 변경하지 않는다.
  artifacts:
    - skills/skill-evolver/scripts/evolver.py
    - skills/skill-evolver/tests/test_capture.py
    - skills/skill-evolver/references/runtime.json
    - skills/skill-evolver/SKILL.md
    - hooks/hooks.json
    - README.md
    - docs/release-reports/runtime-queue.json
  key_links:
    - docs/feasibility-report-v2.json -> docs/release-reports/runtime-queue.json
    - hooks/hooks.json -> skills/skill-evolver/scripts/evolver.py
    - skills/skill-evolver/references/runtime.json -> skills/skill-evolver/scripts/evolver.py
    - skills/skill-evolver/scripts/evolver.py -> docs/release-reports/runtime-queue.json
---

<objective>
Phase 2의 session-level PASS 계약을 production Runtime Queue로 구현하고,
transcript-free read-only status와 immutable completion evidence를 제공한다.

Purpose: 명시적 Review 단계가 사용할 수 있는 silent, bounded, privacy-aware
session queue를 만든다.
Output: schema-v1 SQLite runtime, trusted Stop Hook, bounded spool/maintenance,
operations runbook, canonical PASS report.
</objective>

<historical_import>
이 GSD 계획은 이미 실행된 Superpowers 계획
`docs/superpowers/plans/2026-07-28-skill-evolver-session-runtime-queue.md`
의 관리 기록이다. 다시 실행하지 않는다.

구현은 Task 1 `50b7727`부터 Task 7 운영 보고서와 계획 동기화
`d7fb9b9`, `bdb3a94`, `9fc3a68`까지 완료됐다. 각 task는 TDD와 독립
review를 거쳤고 최종 review는 `CLEAN`이다.
</historical_import>

<execution_evidence>
- Frozen Phase 2 harness baseline: `50b7727`
- Installation/schema foundation and capture hardening: `3ca61ec`,
  `6c751e0`, `1967884`
- Stop Hook and bounded fallback: `99b56ce`, `17e7710`
- Production activation and command sequencing: `15c7696`, `cda934e`, `7959453`
- Generation/epoch/frozen-boundary transaction: `0776601`, `c533681`,
  `1b68df6`, `4dd5bd8`
- Retention, spool privacy and read-only status: `0e8c409`, `8f9a0f1`
- Operations and completion evidence: `d7fb9b9`, `bdb3a94`, `9fc3a68`
</execution_evidence>

<verification>
- `/usr/bin/python3 -m unittest discover -s skills/skill-evolver/tests -p 'test_*.py'`
  ran 253 tests and returned `OK (skipped=3)`.
- `docs/release-reports/runtime-queue.json` is `PASS`, binds implementation
  commit `d7fb9b95d8f5816fde13a274a9b421c7b69412c7`, and contains 12 true
  checks plus seven exact production digests.
- The report binds the Phase 2 gate and predecessor digest
  `ced4503adb44bd041de063c04e0c6c64d0831370fc12e96a920fe97244d8ae15`.
- JSON validation, privacy-field scan, worktree/commit digest comparison,
  `git diff --check`, and independent review all passed.
</verification>

<success_criteria>
- CAPT-01 behavior is implemented and covered by the current deterministic suite.
- Runtime completion evidence is committed at the canonical downstream path.
- Review remains explicit and unavailable until Phase 4 completes.
- No installed skill, staging tree, snapshot, model, or network mutation is
  introduced by the automatic Stop path.
</success_criteria>
